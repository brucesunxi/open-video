# 2026-09-15 本地回归检查

本轮检查未发现自动化覆盖范围内的新故障。不能将此结论等同于线上全链路验收。

## 通过的检查

- `python -m pytest -q`：34 项通过。涵盖教师资料隔离、课程版本与发布、课堂状态与过期 revision、取消后迟到回答、登录退出和限流、素材与音色归属校验、PPTX 导出、重启状态恢复、语音服务接口契约、流顺序与错误、课程媒体缓存、MP4 无重编码合并和 Range 读取。
- `node scripts/speech-pipeline-test.mjs`：通过。涵盖完整文本分段、网络半包解析、顺序播放、取消、异常/截断流、预备视频直播放与预加载复用、板书片段时间映射。
- `node scripts/board-preview-test.mjs`：两套课程共 18 页通过。涵盖初始三条预览、第一条正式板书出现时切换、讲解完成后全部内容、静音阅读和空内容。
- `npm run build`：TypeScript 检查和生产构建通过。保留现有大于 500 kB 的 bundle 提示。
- `git diff --check`：通过。

## 未完成的验证

本地无头 Chrome 在沙箱内启动失败；申请浏览器执行权限后，自动审批因会话检查点与审批模型不兼容而拒绝。因此本轮未运行真实浏览器 UI、手机像素排版和媒体解码测试。SSR 渲染检查不代替视觉验收。

服务器上传也受到相同审批故障阻挡。本轮未直接测试线上 GPU 合成、麦克风、手机浏览器权限、实时对话实际延迟或公网播放。用户提供的四项服务健康状态为就绪，但不能替代本轮端到端测试。

## 本轮修改

新增课前黑板预览：显示当前页前三条有效板书内容，保留分数排版；首条正式板书出现后使用原随讲板书。声音、讲稿、模型和缓存身份保持原逻辑。
