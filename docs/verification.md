# 本地开发版验证记录

验证日期：2026-09-11。平台：macOS，本地 CPU 业务服务。

- `npm run build`：通过。存在 3D 依赖导致的单包体积提示，后续可以按需加载优化。
- `.venv/bin/python -m pytest -q`：14 项通过。覆盖教师资料归属、审核、课程冻结、会话取消竞态、恢复、访问校验、可编辑 PPTX 与模型 HTTP 协议。
- `npm run test:ui`：Chrome 通过。覆盖课堂启动、翻页、插问、续讲、页面导航、PPT 下载、刷新恢复进度和手机布局。
- 人工检查桌面和手机测试截图：布局可用，无水平溢出。

浏览器自动测试使用静音模式，没有将测试通过解释为真实声音播放或模型效果验收。GPU 模型、教师音色相似度、真人流、真实 VRM 素材和长时间连续授课仍需实机测试。Docker 构建尚未验证。第三方依赖审计见 third-party.md。

体验服务：`http://127.0.0.1:8010`。测试服务使用独立临时数据库，不包含真实教师素材。

实时对话补充验证：`node scripts/realtime-smoke.mjs` 通过，模拟浏览器识别与朗读，验证自动发送、答完继续听、按钮打断、结束及切换模式清理。未将模拟通过当作真实麦克风、厂商识别服务或 GPU 延迟验收。

自动 ASR 兜底：六项 Chrome 模拟场景通过（网络错误、权限拒绝、未配置 GPU、GPU 返回失败、正常静音、浏览器识别超时）。真实手机与 GPU 音频链路尚未验收。
